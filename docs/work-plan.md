# תוכנית עבודה — עד ההצגה (יום שני 31.08.26, 16:00)

חלון זמן כולל: כ-36 שעות מרגע כתיבת מסמך זה. שלושה חברי צוות.

## שלב 0 — סגירת החלטות (עד הערב)

- מעבר על פרק 10 (ריכוז שאלות והחלטות פתוחות) כצוות, וסגירת סעיפים 1–3 לפחות.
- הגדרת דאטה לדמו: שימוש בקובץ ה-Markdown המרוכז שכבר קיים ממערכות הפעלה (חוסך זמן חילוץ ידני).
- חלוקת תפקידים ראשונית בין 3 חברי הצוות (הצעה: (א) קליטה+חילוץ קלט, (ב) מנוע סיווג/איחוד, (ג) ייצוא מסמך+ממשק).

## שלב 1 — תכנון עם BMad Method

BMad הוא פריימוורק פתוח-קוד (MIT) לתכנון agile מונחה-AI, עם סוכני AI ייעודיים (Analyst, PM, Architect) ותהליך מובנה: ניתוח → תכנון → ארכיטקטורה → מסירה למימוש.

צעדים מוצעים:
- הרצת שלב ניתוח/PRD (ניתן להזין את מסמך ה-PRD הזה כ-Brief קיים, כדי לחסוך את שלב האנליזה מאפס).
- הרצת שלב Architecture, שם ייסגרו ההחלטות הטכניות מפרק 06 (בחירת LLM, Backend, ספריית מסמכים).
- יעד זמן: סיום עד תחילת שלב המימוש (מומלץ תוך 2–3 שעות).

## שלב 2 — מימוש עם Superpowers

Superpowers הוא פלאגין ל-Claude Code שמטמיע מתודולוגיית פיתוח מובנית (14 skills): סיעור מוחות → מפרט → פיתוח מבודד (git worktrees) → TDD (כתיבת בדיקה כושלת לפני קוד) → בדיקת קוד.

חלוקה מוצעת ל-3 זרמי עבודה מקבילים (תואם לעקרון ה-worktrees של הכלי, שנועד למנוע התנגשויות בין מפתחים):
- זרם א: חילוץ טקסט מ-PDF + ממשק העלאה.
- זרם ב: מנוע סיווג/איחוד (קריאות ל-LLM, לוגיקת דדופ).
- זרם ג: יצירת מסמך פלט (Word/PDF) + ממשק תצוגה מקדימה.

## שלב 3 — אינטגרציה ופריסה

- חיבור שלושת הזרמים למוצר אחד.
- פריסה ל-Railway (לפי המלצת המנחה).
- בדיקת קצה-לקצה מלאה על דאטה אמיתי (מבחני מערכות הפעלה).

## שלב 4 — הכנה להצגה

- README ברור ב-GitHub (בעיה, פתרון, טכנולוגיות, הדגמה).
- הכנת תרחיש הדגמה קצר (מתחת ל-3 דקות) התואם לקריטריוני ההצלחה בפרק 09.
- טיוטת פוסט LinkedIn (לאחר ההצגה).

## תזכורות תפעוליות מהמנחה

- לשים לב לניצול טוקנים/trial period של חברי הצוות.
- לשים לב לגודל ה-context window תוך כדי עבודה (בפרט בהעלאת קבצי PDF מרובים).
- להימנע מתלויות/ספריות לא בשלות (פחות מ-300 כוכבים ב-GitHub).
- פחות מחשבה, יותר עשייה — להתכנס לתוצאה עובדת מהר.

## מקורות
- Superpowers — תיאור רשמי: https://claude.com/plugins/superpowers
- BMad Method — מאגר רשמי: https://github.com/bmad-code-org/BMAD-METHOD
- הנחיות המנחה כפי שהועברו בהודעת הסדנה (מצוטטות בפנייה המקורית של המשתמשת).

**נדרשת החלטה:** אישור חלוקת התפקידים/הזרמים המוצעת לעיל בין שלושת חברי הצוות בפועל, בהתאם לחוזקות כל אחד/ת.
